//! `alidade` — the CLI driver for the engine and store crates, and the
//! acceptance harness for the whole engine. Real subcommands
//! (`single|continuous|ping|export|probe-targets`) land in later tasks;
//! this is a workspace-scaffold stub (plan 052 / task 1).

fn main() {
    println!("alidade: workspace scaffold only, no commands yet (plan 052 task 1)");
}
